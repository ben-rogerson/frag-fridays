node_modules
.env
.env.*
.DS_Store
valve.zip
**/valve.zip
server/**/steam-files/
*.log
dist
*.tsbuildinfo
.impeccable

# YaPB runtime output - bots write learned data + logs here on the box,
# and pull.sh would otherwise drag it into the repo
server/**/addons/yapb/data/logs/
server/**/addons/yapb/data/pwf/
server/**/addons/yapb/data/train/

# built web client (apps/web -> server/web via deploy.sh)
server/web/
__pycache__/

# season kill-log mirror (scripts/standings.sh) - local only, 50MB+ of churn
data/logs/
