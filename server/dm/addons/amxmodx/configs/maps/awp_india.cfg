// awp_india: same one-weapon setup as cs_deagle5 - game_player_equip strips
// everyone on spawn and hands out AWP + armour, info_map_parameters says
// buying 3. The DLL ignores the buying rule, so frag_dm enforces it: AWP
// replaces the DM kit and anything else is stripped on deploy. amxx.cfg
// resets dm_only every map start so this does not leak to other maps.
dm_only "awp"
